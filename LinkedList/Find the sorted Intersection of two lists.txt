class Result {
  static Node findIntersection(Node headA, Node headB) {
Set<Integer> nodes = new HashSet<>();
    Node org=new Node(0);
        Node pb = headB,temp=org;
        while (pb != null) {
            nodes.add(pb.data);
            pb = pb.next;
        }
        if (nodes.isEmpty()) {
            return null;
        }
        Node pa = headA;
        while (pa != null) {
            if (nodes.contains(pa.data)) {
                temp.next=pa;
              temp=temp.next;
            }
            pa = pa.next;
        }
    temp.next=null;
        return org.next;
  }
}