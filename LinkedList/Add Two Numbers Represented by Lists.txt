/* struct Node
{
    int data;
    struct Node* next;
};

Above structure is used to define the linked list, You have to complete the below functions only */

struct Node* addListNumbers(struct Node* head1, struct Node* head2) 
{
  struct Node* start1=head1;
  struct Node* start2=head2;
  
  struct Node* prev=NULL;
  struct Node* n=NULL;
  struct Node* current=start1;
  
  while(current)
  {
    n=current->next;
    current->next=prev;
    prev=current;
    current=n;
  }
  start1=prev;
  
  int num1=0;  
  while(start1)
  {
    num1=num1*10+start1->data;
    start1=start1->next;
  }
  
  prev=NULL;
  n=NULL;
  current=start2;
  
  while(current)
  {
    n=current->next;
    current->next=prev;
    prev=current;
    current=n;
  }
  start2=prev;
  
  int num2=0;  
  while(start2)
  {
    num2=num2*10+start2->data;
    start2=start2->next;
  }
  
  int sum=num1+num2,rem;
  while(sum>0)
  {
    rem=sum%10;
    printf("%d ",rem);
    sum=sum/10;
  }
  
  return NULL;
}