Input: s = "Hi World"
Output: 5
Example 2:

Input: s = " "
Output: 0

class Solution {
    public int lengthOfLastWord(String s) {
        String arr[]=s.split(" ");
        if(arr.length>0)
            return arr[arr.length-1].length();
        else
            return 0;
        
    }
}