Node compute(Node head)
    {
        Node rev=reverse(head);
        Node curr=rev,max=rev;
        while(curr!=null && curr.next!=null)
        {
            if(curr.next.data<max.data)
            {
               Node temp=curr.next;
               curr.next=temp.next;
               temp=null;
            }
            else
            {
                curr=curr.next;
                max=curr;
            }
        }
        head=reverse(rev);
        return head;
    }

Node reverse(Node head)
    {
        Node curr= head,prev = null,next;
        while (current != null) {
            next = curr.next;
            curr.next = prev;
            prev = current;
            current = next;
        }
        return prev;
    }